<Window x:Class="EasySave.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySave"
        xmlns:views="clr-namespace:EasySave.View"
        xmlns:viewmodels="clr-namespace:EasySave.ViewModel"
        mc:Ignorable="d"
        Title="EasySave" Height="475" Width="800">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type viewmodels:MenuViewModel}">
                <views:Menu />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:LanguageViewModel}">
                <views:Language />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:CreateBackupViewModel}">
                <views:CreateBackup />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:InfoBackupViewModel}">
                <views:InfoBackup />
            </DataTemplate>
        </Grid.Resources>
        <Button Content="{Binding TMenu}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="25" Width="50" Command="{Binding UpdateViewCommand}" CommandParameter="Menu"/>
        <ContentControl Content="{Binding SelectedViewModel}" Margin="0,25,0,0" />
    </Grid>
</Window>
